
Key notes:-
> Method1: Display index and parent index using $index and $parent.$index
	>> Note that invalid html does not display properly. No errors r reported.
> Method2: Display index and parent index using ng-init="parentIndex=$index", where parentIndex is a variable which we have defined.
	>> {{parentIndex}} can be used in the view.

goto http://127.0.0.1/z_radical/4.html on chrome.
