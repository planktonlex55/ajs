Pre-requisite:-
While using notepad++ Settings -> Preferences -> Auto-Completion -> and check the "html/xml close tag" checkbox.
What this does is it adds ending tag.

Key notes:-
> Model is a javascript array
> ng-repeat
> bootstrapping the table in view.

goto http://127.0.0.1/z_radical/2.html on chrome.
O/p:- is bootstrapped table showing all employees.
